<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    >

    <ListView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/allMessages"
        android:paddingLeft="3dp"
        android:paddingRight="3dp"
        android:paddingBottom="4dp"
        android:layout_above="@+id/editTextSendNewMess"
        />


    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/editTextSendNewMess"
        android:hint="Сообщение"
        android:layout_gravity="bottom"
        android:layout_weight="0.98"
        android:layout_alignBottom="@+id/btnSendNewMess"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_toLeftOf="@+id/btnSendNewMess"
        android:layout_toStartOf="@+id/btnSendNewMess" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Send"
        android:id="@+id/btnSendNewMess"
        android:layout_gravity="bottom"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:background="@drawable/btn_green"
        android:layout_alignParentEnd="true" />
</RelativeLayout>